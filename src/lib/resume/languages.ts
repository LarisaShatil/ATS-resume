import type { SpokenLanguageEntry } from "./types";

/** Default when parsing or initializing sections (no auto-detect yet). */
export function defaultLanguageRows(): SpokenLanguageEntry[] {
  return [];
}
